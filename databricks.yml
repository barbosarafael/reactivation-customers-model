bundle:
  name: reactivation-customers-model

include:
  - resources/*.yml

variables:
  sql_warehouse_id:
    description: ID do SQL Warehouse usado pelo dashboard.

targets:
  dev:
    default: true
    mode: development

    workspace:
      host: https://dbc-0bf2fbec-8742.cloud.databricks.com
      root_path: "~/.bundle/${bundle.name}/${bundle.target}"

    variables:
      sql_warehouse_id: ad4e58affc95c88e
